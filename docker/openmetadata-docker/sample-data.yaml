source:
  type: sample-data
  serviceName: local_sample_data
  serviceConnection:
    config:
      type: SampleData
      sampleDataFolder: "./examples/sample_data"
  sourceConfig:
    config:
      type: MetadataPipeline  # <-- Critical: Specifies the pipeline type
      # Removed specific 'include' flags as the sample-data connector often uses defaults
      # or automatically includes all assets based on the MetadataPipeline type.
processor:
  type: all-assets
  config: {}
sink:
  type: metadata-rest
  config: {}
workflowConfig:
  openMetadataServerConfig:
    hostPort: http://localhost:8585
    authProvider: no-auth
